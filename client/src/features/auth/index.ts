// Auth feature exports
export { useAuth } from './hooks/useAuth';
export { isUnauthorizedError } from './utils/authUtils';
export type { AuthUser, AuthState, LoginCredentials, RegisterData } from './types';